import { Routes, Route } from "react-router-dom";

const Navbar = () => {
  return (
    <nav className="Navbar">
      <span>Profile</span>
      <span>Settings</span>
      <span>Favorites</span>
    </nav>
  );
};

export default Navbar;
